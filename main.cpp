#include <iostream>
#include <vector>

using namespace std;

int main()
{
    cout << "Hello, World!" << endl;

    vector<pair> data = new vector();
    data.push_back(pair(2,10));
    data.push_back(pair(2,5));
    data.push_back(pair(8,4));
    data.push_back(pair(2,10));
    data.push_back(pair(2,10));
    data.push_back(pair(2,10));
    data.push_back(pair(2,10));

    

    return 0;
}